#pragma once

// Copy this file to secrets.h and fill in your network + WS hub.
#ifndef VISION_MENU_SECRETS_H
#define VISION_MENU_SECRETS_H

static const char *WIFI_SSID = "YOUR_WIFI_SSID";
static const char *WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// LAN IP of the machine running `pnpm --filter web dev`
static const char *WS_HOST = "192.168.1.100";
static const uint16_t WS_PORT = 3001;

static const char *DEVICE_ID = "xiao-vision-01";

#endif
