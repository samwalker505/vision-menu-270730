# Copy to .env.local for local overrides (optional for v1).
# Ingest is open on the LAN — do not expose publicly without auth.

# Optional future: shared store for multi-instance deploys
# REDIS_URL=
